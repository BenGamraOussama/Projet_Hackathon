spring.application.name=astba
spring.datasource.url=jdbc:mysql://localhost:3306/astba?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
server.port=8080

# Flyway
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=4

# JWT Secret (In production this should be in env var)
# Min 512 bits (64 bytes) for HS512, min 256 bits (32 bytes) for HS256
jwt.secret=aVeryLongSecretKeyForHS512AlgorithmThatIsAtLeast512BitsLongForSecurityCompliance2024ConfigurationWithMoreData
jwt.expiration=86400000

# SMTP (set env vars to enable email sending)
spring.mail.host: smtp.gmail.com
spring.mail.port: 587
spring.mail.username: benibrahim.anas27@gmail.com
spring.mail.password: bdha ooar fqma xxkl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=${SMTP_FROM:no-reply@astba.tn}

# LLM (Mistral) configuration
llm.provider=${LLM_PROVIDER:mistral}
llm.mistral.api-key=${MISTRAL_API_KEY:}
llm.mistral.base-url=${MISTRAL_BASE_URL:https://api.mistral.ai}
llm.mistral.model=${MISTRAL_MODEL:mistral-large-latest}
llm.mistral.timeout-ms=${MISTRAL_TIMEOUT_MS:20000}
llm.mistral.max-tokens=${MISTRAL_MAX_TOKENS:2500}
llm.mistral.temperature=${MISTRAL_TEMPERATURE:0.2}
llm.mistral.stub-enabled=${MISTRAL_STUB_ENABLED:false}
